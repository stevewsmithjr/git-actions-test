#include "main.h"

void main(int argc, char **argv) {
    int test
    int test_int = 100;
    print_int(test_int);
    
    if (1) {
        printf("the curly brace should be on a new line!\n%d",test_int);
    }

    return 0;
}